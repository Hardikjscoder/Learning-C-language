#include <stdio.h> 

int main() {
    // Basics of array
    int marks[4];
    marks[0] = 1;
    marks[1] = 2;
    marks[2] = 3;
    marks[3] = 4;

    printf("The marks of students are: %d %d %d %d", marks[0], marks[1], marks[2], marks[3]);

    return 0;
}

/*

An array is a collection of similar elements.
One variable => capable of storing multiple values

IMP = Index of array in almost all languages starts with 0.

*/